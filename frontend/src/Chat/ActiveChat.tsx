import React from 'react'
import styled from 'styled-components'

const StyledActiveChat = styled.div`
  flex-grow: 1;
  padding: 18px;
`

interface Props {}

const ActiveChat = ({  }: Props) => {
  return <StyledActiveChat />
}

export default ActiveChat
